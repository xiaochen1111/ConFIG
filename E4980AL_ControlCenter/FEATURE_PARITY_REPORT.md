# 功能补全核对（相对原始单文件版本）

## 已具备（当前模块化版本）
- 入口装配：状态、UI、设备服务、测量服务、采样服务、曲线服务。
- 连接/断开/读 IDN 基础流程。
- 测量服务增强 API（含 `applySettings`、`fetchOnce`、`readIDN`、`rawQuery` 等）。
- 采样服务定时器与数据缓存骨架。

## 本次补齐
- 控制页补充 `txtIDN` 句柄并回填到 `appUi`，保证 `onReadIDN` 可写入显示。
- 采样页补充 `txtLog` 句柄并回填到 `appUi`，保证日志输出可见。

## 仍待补齐（与原始完整版相比）
- 控制页尚未恢复完整参数控件（频率/电平/参数对/速度/触发下拉等）的事件联动。
- 原始 SCPI 调试区（命令输入、Query/Write 按钮、返回框）尚未完整恢复到新 UI。
- 采样页的开始/停止/保存 CSV/清空数据按钮联动尚未全部接线到新服务层。
- 设置页与会话配置双向同步逻辑尚未全部回填。
- 导航页标题、副标题、按钮高亮、品牌展示细节仍需 1:1 对齐原版。

## 结论
当前版本是“可运行的结构化骨架 + 核心通信/测量能力”。
如需“与原单文件功能 1:1 补全”，还需要继续完成 UI 控件层与 actions 的全面迁移。
